package com.photo.mixer.app;

import android.os.Environment;

public class ProjectUtils {
    public static String savedPaths = Environment.getExternalStorageDirectory().getAbsolutePath() + "/Blend Photos and Editor/";
}